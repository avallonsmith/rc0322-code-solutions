/* exported last */
function last(array) {
  var $last = (array.length - 1);
  var lastArray = array[$last];
  return lastArray;
}

// create a new function namned 'rush'
// give it one parameter 'array'
// declare a new variable '$last' and assign it a value of the array's length minus one to get last element in the array
// delcare a new variable 'lastArray' and assign it a value of the array at the last index
// return the varible 'lastArray'
