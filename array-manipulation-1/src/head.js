/* exported head */
function head(array) {
  var $head = array[0];
  return $head;
}

// create a new function named 'head'
// give function one parameter 'array'
// declare a variable '$head' and assign it a value of an array at the first index
// return the variable '$head'
